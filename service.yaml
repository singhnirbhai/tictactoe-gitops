#apiVersion: v1
#kind: Service
#metadata:
 # name: tictactoe-service
#spec:
 # type: NodePort
  #selector:
   # app: tictactoe
  #ports:
  #  - port: 80
   #   targetPort: 80
    #  nodePort: 30007

apiVersion: v1
kind: Service
metadata:
  name: netflix-app-service
spec:
  type: NodePort
  selector:
    app: netflix-app
  ports:
  - port: 5173
    targetPort: 5173
    nodePort: 30080
